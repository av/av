+modal('bip-portfomance', "Portfomance 3")
  p Third iteration of performance work on the Portfolios feature — a particularly challenging engineering constraint due to the decision of loading an entire organization's portfolio tree at once.
  p Achieved a 3.84× average improvement in page load time, with one large client going from 60+ second timeouts to ~11 seconds. Portfolio sharing DB performance improved ~10×.
  p Also introduced a backend integration test suite (40+ tests using testcontainers) that made the changes testable at all — manual testing would have taken months.
  p CI pipeline cost reduced 3× without sacrificing runtime.
